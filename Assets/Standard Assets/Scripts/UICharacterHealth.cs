using UnityEngine;
using UnityEngine.UI;
using CreatorKitCode;

[RequireComponent(typeof(Slider))]
public class UICharacterHealth : MonoBehaviour
{
	// public CharacterData data;

	// Slider slider;
	// private void Start()
	// {
	// 	slider = GetComponent<Slider>();
	// }
	// private void Update()
	// {
	// 	if (data && slider)
	// 	{
	// 		slider.value = data.Stats.CurrentHealth / (float)data.Stats.stats.health;
	// 		// MaxHealth.text = PlayerCharacter.Data.Stats.stats.health.ToString();
	// 		// CurrentHealth.text = PlayerCharacter.Data.Stats.CurrentHealth.ToString();
	// 	}
	// }
}